---
title: Examples
sidebarTitle: Overview
description: "Copy complete Deckflow recipes with a known input, runnable command, expected output, and verification step."
---

<div className="deckflow-hero">
  <span className="deckflow-hero-eyebrow">Copy, run, then adapt</span>
  <h2 className="deckflow-hero-title">Start from a complete document task</h2>
  <p className="deckflow-hero-copy">Examples are deliberately narrower than product guides: each one begins with a known input and ends with a result you can verify before using your own documents.</p>
</div>

<Columns cols={2}>
  <Card title="Inspect and route an uploaded PDF" icon="shield-halved" iconType="solid" color="#14b8a6" href="/examples/inspect-and-route" cta="Run the example" arrow>
    Use DeckProbe locally, read Schema v2 facts, and turn them into an application routing decision.
  </Card>
  <Card title="Generate an editable PPTX from HTML" icon="file-powerpoint" iconType="solid" color="#ec4899" href="/examples/html-to-pptx" cta="Run the example" arrow>
    Create a one-slide HTML source, convert it locally with DeckHTML, and verify the PPTX artifact.
  </Card>
</Columns>

## How examples differ from other pages

| Page | Use it to answer |
| --- | --- |
| [Getting Started](/getting-started) | What is the shortest first result for each product? |
| [Choose a Product](/product-guide) | Which product owns the output I need? |
| [Common Workflows](/common-workflows) | How do products fit into an application or combine? |
| Examples | What files and commands can I copy to complete one concrete task? |

<Info>
  The example set will grow with verified product capabilities. An example is added only when its public package, input, command, and success criteria can be tested as one continuous path.
</Info>
