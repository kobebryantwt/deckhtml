---
title: Examples
sidebarTitle: Overview
description: "Copy complete DeckFlow recipes with a known input, runnable command, expected output, and verification step."
---

Each example begins with a known input and ends with a result you can verify. Run the complete path first, then replace the sample with your own document or source.

<Columns cols={2}>
  <Card title="Inspect and route an uploaded PDF" icon="shield-halved" iconType="solid" href="/examples/inspect-and-route" cta="Run the example" arrow>
    Use DeckProbe locally, read Schema v2 facts, and turn them into an application routing decision.
  </Card>
  <Card title="Generate an editable PPTX from HTML" icon="file-powerpoint" iconType="solid" href="/examples/html-to-pptx" cta="Run the example" arrow>
    Create a one-slide HTML source, convert it locally with DeckHTML, and verify the PPTX artifact.
  </Card>
  <Card title="Render HTML to a PNG preview" icon="images" iconType="solid" href="/examples/render-html-preview" cta="Run the example" arrow>
    Render a known HTML page through DeckFlow cloud, capture the JSON result, and verify the PNG signature.
  </Card>
  <Card title="Update approved content in a PPTX" icon="pen-to-square" iconType="solid" href="/examples/update-pptx-content" cta="Run the example" arrow>
    Use DeckUse locally to query a known presentation, apply a reviewed JSON change, validate it, and verify the exported PPTX.
  </Card>
</Columns>

<Info>
  Use [Getting Started](/getting-started) for the shortest result from each product, and [Common Workflows](/common-workflows) when you need to combine products. This section is reserved for complete, copy-and-run tasks whose installation path, input, command, and success criteria can be tested as one path.
</Info>
