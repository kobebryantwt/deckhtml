---
title: Choose a Product
sidebarTitle: Choose a Product
description: "Compare Deckflow products by input, operation, execution location, and final artifact."
---

<div className="deckflow-hero">
  <span className="deckflow-hero-eyebrow">Choose by the final contract</span>
  <h2 className="deckflow-hero-title">Pick the product that owns your result</h2>
  <p className="deckflow-hero-copy">Start with what the application must receive—facts, pixels, structured content, an updated source file, or a new deck—and then confirm where it runs.</p>
</div>

<Columns cols={3}>
  <Card title="Facts" icon="magnifying-glass" iconType="solid" color="#14b8a6" href="/deckprobe/overview">DeckProbe</Card>
  <Card title="Pixels" icon="images" iconType="solid" color="#3b82f6" href="/deckrender/overview">DeckRender</Card>
  <Card title="Structured results" icon="cube" iconType="solid" color="#8b5cf6" href="/deckops/overview">DeckOps</Card>
  <Card title="Updated PPTX" icon="pen-to-square" iconType="solid" color="#f59e0b" href="/deckuse/overview">DeckUse</Card>
  <Card title="New deck" icon="code" iconType="solid" color="#ec4899" href="/deckhtml/overview">DeckHTML</Card>
</Columns>

## Product comparison

| Product | Start with | Produces | Runs | Does not replace |
| --- | --- | --- | --- | --- |
| DeckProbe | Existing PDF, Office, or modern iWork document | Schema v2 target evidence and measured probe cost | Native Rust CLI; WebAssembly source awaiting corrected npm artifact | Full content parsing, rendering, or editing |
| DeckRender | Supported document, URL, or stdin | Images, PDF, or video | Local, passthrough, or cloud route | Structured content extraction or source editing |
| DeckOps | File, URL, or uploaded file ID | Format-specific parse results, Markdown, or generated files | Deckflow cloud tasks | Local-only inspection or editable source mutation |
| DeckUse | Existing PPTX | Updated PPTX | Local Node.js workflow | DOCX/XLSX/Keynote editing or new deck authoring |
| DeckHTML | HTML, SVG, URL, or stdin | PPTX, PNG, and conversion diagnostics | Local browser or cloud task | General-purpose conversion of existing Office files |

## Common decisions

<AccordionGroup>
  <Accordion title="DeckProbe or DeckOps?">
    Use <strong>DeckProbe</strong> when verified document-level facts are the final result or are needed before accepting and routing a file. Use <strong>DeckOps</strong> when you need page and element content, Markdown, semantic extraction, or a generated file. A deeper DeckProbe level still returns target evidence, not a DeckOps parse result.
  </Accordion>
  <Accordion title="DeckRender or DeckOps?">
    Use <strong>DeckRender</strong> when your application needs a predictable visual artifact and does not want to manage underlying cloud task names, multi-step routes, page naming, or downloads. Use <strong>DeckOps</strong> when the task itself is part of a broader parse, extract, convert, or generation workflow.
  </Accordion>
  <Accordion title="DeckUse or DeckHTML?">
    Use <strong>DeckUse</strong> when an existing PPTX is the source of truth and you need to change its current content. Use <strong>DeckHTML</strong> when HTML or SVG is the authoring source and a new PPTX or PNG is the result.
  </Accordion>
  <Accordion title="Can products be combined?">
    Yes. A typical ingestion flow can use DeckProbe to validate a file, DeckOps to parse it, and DeckRender to produce previews. These are separate calls with separate result contracts; Deckflow does not require every workflow to use all products.
  </Accordion>
</AccordionGroup>

## Move from selection to implementation

<Card title="Common workflows" href="/common-workflows">
  Follow result-oriented application paths for upload validation, search and RAG, review previews, PPTX personalization, HTML-authored decks, and multi-product ingestion.
</Card>

<Warning>
  Product names describe responsibility, not identical format coverage or package availability. Check the selected product's format matrix and availability warning before designing a workflow; DeckProbe JavaScript and DeckOps Parse source APIs currently await corrected public artifacts.
</Warning>
