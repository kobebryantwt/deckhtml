---
title: Overview
sidebarTitle: Overview
description: "Use the Deckflow API reference to understand the common async task flow, the core task endpoints, and the scenario-specific parameters built on top of them."
---

Deckflow's API is organized around a common async task flow. Business scenarios such as generation, translation, revamp, and tool APIs all reuse the same submission and task-management pattern.

At the reference level, the most important shared endpoints are:

- `POST /tools/tasks`
- `GET /tools/tasks/{id}`
- `GET /tools/tasks/{id}/download`

This section is therefore split into two layers:

- **Core APIs**: submit a task, query its status, and download the finished result
- **Scenario guides**: understand which `type`, `files`, and `params` fields to send for each business workflow

<Note>
  The phrase "common async task flow" is a descriptive label used in this guide for the shared task lifecycle and authentication model. It is not a separate product term.
</Note>

## Common async task flow

Deckflow uses the same lifecycle across business scenarios:

- Authenticate with `Authorization: Bearer <YOUR_API_KEY>`.
- Submit files, task type, and JSON options as `multipart/form-data`.
- Receive a task identifier immediately.
- Poll the task endpoint or subscribe through SSE until the job is complete or failed.
- Download the final file from `result.url` or from the dedicated download endpoint.

## Read this section by group

<CardGroup cols={3}>
  <Card title="Authentication & Task Model" href="/reference/authentication-and-task-model">
    Review the base URL, headers, common submit fields, and the shared task lifecycle.
  </Card>
  <Card title="Submit Async Task" href="/reference/submit-async-task">
    Inspect the unified `POST /tools/tasks` contract and try it in the playground.
  </Card>
  <Card title="Query Task Status" href="/reference/query-task-status">
    Inspect the unified `GET /tools/tasks/{id}` contract, including polling and SSE usage.
  </Card>
  <Card title="Download Task Result" href="/reference/download-task-result">
    Inspect the direct download endpoint used after a task has completed successfully.
  </Card>
  <Card title="Generation Scenarios" href="/reference/generation-scenarios">
    Review every generation field, including the full `params` table.
  </Card>
  <Card title="Translation Scenarios" href="/reference/translation-scenarios">
    Review the complete translation fields and language parameters.
  </Card>
  <Card title="Revamp Scenarios" href="/reference/revamp-scenarios">
    Review the full revamp request fields and design-guidance parameters.
  </Card>
  <Card title="Tool API Scenarios" href="/reference/tool-api-scenarios">
    Review complete converter, extractor, merger/splitter, and optimizer type tables.
  </Card>
</CardGroup>

<Tip>
  If you only remember one rule, make it this: scenario pages explain **what to send in `type`, `files`, and `params`**, while the core API pages explain **where and how to send it**.
</Tip>
