---
title: 示例
sidebarTitle: 概览
description: "复制带有已知输入、可运行命令、预期输出和验证步骤的完整 DeckFlow 示例。"
---

每个示例都从已知输入开始，以一个可以验证的结果结束。先跑通完整路径，再替换为自己的文档或源代码。

<Columns cols={2}>
  <Card title="检查并路由上传的 PDF" icon="shield-halved" iconType="solid" href="/zh/examples/inspect-and-route" cta="运行示例" arrow>
    在本地运行 DeckProbe，读取 Schema v2 事实，并将其转换为应用路由判断。
  </Card>
  <Card title="从 HTML 生成可编辑 PPTX" icon="file-powerpoint" iconType="solid" href="/zh/examples/html-to-pptx" cta="运行示例" arrow>
    创建单页 HTML，在本地使用 DeckHTML 转换，并验证 PPTX 产物。
  </Card>
  <Card title="将 HTML 渲染为 PNG 预览" icon="images" iconType="solid" href="/zh/examples/render-html-preview" cta="运行示例" arrow>
    通过 DeckFlow 云端渲染一份已知 HTML，保存 JSON 结果，并验证 PNG 文件签名。
  </Card>
</Columns>

<Info>
  需要查看每个产品的最短结果时使用[开始使用](/zh/getting-started)，需要组合多个产品时查看[常见工作流](/zh/common-workflows)。这里只收录公开软件包、输入、命令和成功标准能够作为一条连续路径完成测试的示例。
</Info>
