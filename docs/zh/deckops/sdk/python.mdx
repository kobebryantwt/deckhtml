---
title: 从源码使用 Python SDK
sidebarTitle: Python（源码）
description: "在 Python 中通过 deckops-sdk 同步执行任务、上传、解析和结果获取。"
---

PyPI 没有提供 DeckOps Python 软件包。使用该接口时，请从经过评审的源码版本安装：

```bash
pip install "git+https://github.com/deckflow/deckops.git@<commit>#subdirectory=sdks/python"
```

请把 `<commit>` 替换为应用已验证的准确版本。

```python
import os
from deckops import create_deck

with create_deck(api_key=os.getenv("DECKOPS_API_KEY")) as deck:
    task = deck.convert_ppt_to_pdf(files=["./slides.pptx"], name="slides")
    done = deck.tasks.wait(task["id"])
    result = deck.tasks.down(done["id"])
```

Python 客户端是同步接口，并持有自己的 HTTP 连接池。短生命周期客户端应使用 Context Manager 或主动调用 `close()`。

<Warning>
  执行任务时请配置 API Key 或用户 Token。当前 Python SDK 没有提供启动游客任务所需的 `tasks.start()` 方法。
</Warning>

## 解析为 Markdown

```python
with create_deck(api_key=os.getenv("DECKOPS_API_KEY")) as deck:
    markdown = deck.parse("./slides.pptx")
    detailed = deck.parse_detailed({
        "file_id": "uploaded-file-id",
        "name": "slides.pptx",
    })
```

SDK 还提供底层上传和任务客户端、任务快捷方法、基于 SSE 的等待与轮询回退，以及结构化解析结果转换器。
