---
title: 从源码使用 Go SDK
sidebarTitle: Go（源码）
description: "使用 DeckOps Go SDK 执行任务、上传、解析和结果获取。"
---

Go SDK 已在 `deckflow/deckops` 中实现，但其声明的 Module Path 当前指向无法获取的 `github.com/deckops/deckops/sdks/go` 仓库。请在本地检出经过评审的版本，并临时增加 `replace`：

```go
require github.com/deckops/deckops/sdks/go v0.0.0
replace github.com/deckops/deckops/sdks/go => ../deckops/sdks/go
```

修正后的 Module 发布后，请移除本地替换。

```go
package main

import (
  "context"
  "os"

  deckops "github.com/deckops/deckops/sdks/go"
)

func main() {
  ctx := context.Background()
  deck, err := deckops.New(ctx, deckops.ClientOptions{
    APIKey: os.Getenv("DECKOPS_API_KEY"),
  })
  if err != nil { panic(err) }

  task, err := deck.ConvertPptToPDF(ctx, deckops.TaskShortcutParams{
    Files: []deckops.TaskUploadInput{{
      Input: deckops.UploadInput{Path: "./slides.pptx"},
    }},
  })
  if err != nil { panic(err) }

  _, err = deck.Tasks.Wait(ctx, task.ID, deckops.WaitForTaskOptions{})
  if err != nil { panic(err) }
}
```

Go SDK 提供任务快捷方法、路径/字节/Reader 上传、访客 UUID 持久化、SSE 或轮询等待、Parse 路由，以及结构化结果到 Markdown 的转换器。
