---
title: 选择产品
sidebarTitle: 选择产品
description: "按使用场景、执行方式和输出结果比较 DeckProbe、DeckRender、DeckOps、DeckUse 与 DeckHTML。"
---

五个产品解决不同的文档任务。选择时应关注应用最终需要什么结果，而不只是输入文件的格式。

| 产品 | 适合场景 | 执行方式 | 主要结果 |
| --- | --- | --- | --- |
| DeckProbe | 快速嗅探、上传检查、路由和策略判断 | 本地原生 Rust 或 Browser JS SDK | 带置信度和实际成本的 Target 证据 |
| DeckRender | 专注文档渲染 | 通过 SDK 调用云端渲染 | 页面或幻灯片画面 |
| DeckOps | 完整的文档理解与转换 | 通过 SDK 调用云端执行能力 | Document IR、语义提取结果、转换文件或渲染结果 |
| DeckUse | 源文件感知的 Office 文件操作 | 本地库工作流 | 更新后的 Office 源文件 |
| DeckHTML | HTML-native Deck 创作 | 本地与云端路径 | 从 HTML 生成的演示文稿产物 |

## DeckProbe 还是 DeckOps？

这是最容易混淆的边界：

- 最终结果是文档级事实，例如已验证格式、元数据、数量、安全信号、置信度和探测成本时，选择 **DeckProbe**。
- 需要页面和元素结构、统一 Document IR、语义 Extract、格式 Convert 或组合 Render 时，选择 **DeckOps**。
- DeckProbe 的 `deep` 级别仍然返回 Target 证据，不会生成 DeckOps Document IR。

## DeckRender 还是 DeckOps？

- 只需要渲染后的页面或幻灯片时，选择 **DeckRender**。
- Render 属于更大的流程，并且还需要 Probe、Parse、Extract 或 Convert 时，选择 **DeckOps**。

## DeckOps 还是 DeckUse？

- 将不同格式统一成 IR 并生成派生结果时，选择 **DeckOps**。
- 支持的 Office 文件仍然是可编辑产物，并需要修改或删除已有元素时，选择 **DeckUse**。

## 什么时候使用 DeckHTML？

HTML 是幻灯片结构和视觉设计的源格式时，选择 DeckHTML。它是专项创作路径，不是针对已有 Office 文档的通用 Parser。
