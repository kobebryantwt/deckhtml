---
title: DeckProbe CLI
sidebarTitle: 概览
description: "安装 DeckProbe CLI，完成第一次嗅探，并进入生产接入所需的后续指南。"
---

DeckProbe CLI 在本地运行目标驱动的 Rust 引擎，适合 Shell、CI、服务和批处理工作流，不依赖 Python、JVM、Microsoft Office 或外部 PDF 库。

## 安装

<Tabs>
  <Tab title="macOS 与 Linux">
    ```bash
    curl --proto '=https' --tlsv1.2 -LsSf \
      https://github.com/deckflow/deckprobe/releases/latest/download/deckprobe-installer.sh | sh
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    powershell -ExecutionPolicy ByPass -c "irm https://github.com/deckflow/deckprobe/releases/latest/download/deckprobe-installer.ps1 | iex"
    ```

    如果安装器更新了用户 `Path`，请重启终端。
  </Tab>
  <Tab title="从源码安装">
    需要 Rust 1.88 或更高版本以及 Git。

    ```bash
    cargo install --git https://github.com/deckflow/deckprobe --locked deckprobe
    ```
  </Tab>
</Tabs>

验证可执行文件并查看内置帮助：

```bash
deckprobe --version
deckprobe --help
```

## 完成第一次嗅探

使用默认的 `metadata` 探测层级嗅探本地文档：

```bash
deckprobe report.pdf
```

只请求应用需要的 Target，并格式化 JSON 方便阅读：

```bash
deckprobe -t format,slide_count --pretty deck.pptx
```

DeckProbe 向标准输出写入一份 Schema v2 JSON 报告。成功解析的 Target 包含 Value、置信度、所选 Path 和证据来源；报告还会记录实际执行成本与诊断信息。

## 常见任务

```bash
# 嗅探身份、通用元数据和主要结构
deckprobe -t @summary report.pdf

# 嗅探安全相关信号
deckprobe -t @security upload.docm

# 只返回紧凑的 Values，不返回完整证据记录
deckprobe -t @summary --view values report.pdf

# 预览所选 Path，不执行非 Header Probe Path
deckprobe --plan --pretty report.pdf

# 请求的 Target 未解析时返回非零状态
deckprobe --strict -t format,page_count report.pdf
```

## 按接入任务继续

<CardGroup cols={2}>
  <Card title="输入与 Target" href="/zh/deckprobe/cli/inputs-and-targets">
    使用本地文件、stdin 或 JSONL；选择必选与可选 Target；配置探测层级、置信度、格式选项和预算。
  </Card>
  <Card title="自动化与输出" href="/zh/deckprobe/cli/automation-and-output">
    处理 Full、Values 和错误报告；配置 JSONL、严格模式、退出码、确定性输出和 CI 工作流。
  </Card>
  <Card title="命令参考" href="/zh/deckprobe/cli/command-reference">
    查看全部公开命令、全局参数、Alias、取值、Discovery、Completion 和 Man Page 命令。
  </Card>
  <Card title="输出模型" href="/zh/deckprobe/output-model">
    阅读完整的 Schema v2 契约和 Target 证据模型。
  </Card>
</CardGroup>
