---
title: 配置
sidebarTitle: 配置
description: "持久化 DeckRender CLI 默认值，理解参数优先级，并隔离 CI 配置。"
---

DeckRender 把渲染偏好保存在 `~/.deckrender/config.json`，与 DeckFlow 共享凭证文件分开。

```bash
deckrender config set profile web
deckrender config set engine local
deckrender config set office2html-path /opt/deckflow/office2html
deckrender config set image-format webp
deckrender config set width 2560
deckrender config unset width
deckrender config list
deckrender config path
```

## 支持的键

| 键 | 取值 |
| --- | --- |
| `engine` | `local`、`cloud`、`auto` |
| `office2html-path` | 本地 `office2html` 可执行文件绝对路径 |
| `profile` | `web`、`presentation`、`print`、`thumbnail` |
| `format` | `image`、`pdf`、`video` |
| `image-format` | `png`、`jpg`、`webp` |
| `quality` | `low`、`medium`、`high` |
| `width` | 正数长边像素值 |
| `scale` | 正数缩放倍数 |
| `timeout` | 正数任务等待秒数 |

引擎选择优先级为：

```text
--engine > DECKRENDER_ENGINE > ~/.deckrender/config.json > cloud
```

其他渲染默认值使用：

```text
profile 默认值 < ~/.deckrender/config.json < 显式 CLI 参数
```

Profile 或持久化默认值不能应用到当前路径时，DeckRender 会丢弃该值并给出警告。显式传入的不兼容参数仍会返回 `unsupported_option`。

## 隔离环境

CI 任务或访客模式测试不应继承工作站凭证时，需要同时重定向三个配置目录：

```bash
export DECKFLOW_CONFIG_DIR=/tmp/deckflow-ci
export DECKRENDER_CONFIG_DIR=/tmp/deckrender-ci
export DECKOPS_CONFIG_DIR=/tmp/deckops-ci
deckrender hello.html -o hello.png --json --quiet
```

三个目录都需要设置，避免旧的 DeckOps Token 被选为备用凭证。环境变量的优先级高于配置文件；需要验证真正的访客模式时，还要清理继承的凭证变量：

```bash
unset DECKRENDER_API_KEY DECKFLOW_API_KEY DECKHTML_API_KEY
unset DECKRENDER_TOKEN DECKFLOW_TOKEN
deckrender config list
```

渲染前，`config list` 中的 `api-key` 和 `token` 都应显示为未设置。如果同一 Shell 中的其他进程仍需要这些变量，请在独立 CI Step 或容器中执行，不要直接清理共享环境。

访客模式、登录、API Key、共享凭证和凭证优先级请查看[认证](/zh/deckrender/authentication)。
