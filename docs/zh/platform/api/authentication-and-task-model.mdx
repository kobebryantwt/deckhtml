---
title: 认证与任务模型
sidebarTitle: 认证与任务模型
description: "查看 DeckFlow API 共用的 Base URL、请求头、公共表单字段，以及通用异步任务流程。"
---

DeckFlow 的不同 API 场景，底层都复用同一套基础能力。在看生成、翻译、焕新和工具 API 之前，建议先把这层公共模型理解清楚。

## Base URL

```text
https://app.deckflow.com/api
```

## 必填请求头

| 请求头 | 是否必填 | 作用 |
|---|---|---|
| `Authorization` | 是 | 使用 `Bearer <YOUR_API_KEY>` 进行认证。 |
| `Content-Type` | 提交任务时必填 | 创建任务时必须使用 `multipart/form-data`。 |

## 通用异步任务流程

<Steps>
  <Step title="完成认证">
    在 `Authorization` 请求头中传入 API Key。
  </Step>
  <Step title="提交任务">
    上传一个或多个文件，指定 `type`，并通过 `params` 传递场景参数。
  </Step>
  <Step title="跟踪状态">
    保存返回的任务 ID，随后通过 `GET /tools/tasks/{id}` 轮询，或通过 `response-event-stream: yes` 订阅 SSE 实时流。
  </Step>
  <Step title="下载结果">
    任务完成后，可以直接读取返回结果中的 `result.url`，也可以调用专用下载接口获取最终文件。
  </Step>
</Steps>

## 公共提交字段

这些字段会在生成、翻译、焕新和工具 API 中反复出现：

| 字段 | 是否必填 | 含义 |
|---|---|---|
| `spaceId` | 否 | 目标工作空间或空间 ID。 |
| `files` | 取决于场景 | 待处理源文件。不同场景对文件数量要求不同，合并类任务通常至少需要两个文件。 |
| `type` | 是 | 业务场景或工具类型标识，例如 `generation`、`translation`、`revamp`、`convertor.html2pptx`。 |
| `name` | 否 | 任务显示名称。 |
| `notifyURL` | 否 | 任务状态变更时的 Webhook 回调地址。 |
| `params` | 通常建议传 | 包含场景参数的 JSON 字符串。 |

## 状态处理

- 任务创建后，通常会先进入 `pending` 或其他非终态。
- 继续查询，直到任务进入 `completed` 或 `failed`。
- 成功任务会返回结果元数据和下载链接。
- 失败任务会返回错误信息，便于排查。

<CardGroup cols={3}>
  <Card title="提交异步任务" href="/zh/platform/api/submit-async-task">
    打开 `POST /tools/tasks` 的核心接口页。
  </Card>
  <Card title="查询任务状态" href="/zh/platform/api/query-task-status">
    打开 `GET /tools/tasks/{id}` 的核心接口页。
  </Card>
  <Card title="下载任务结果" href="/zh/platform/api/download-task-result">
    打开专用结果下载接口页。
  </Card>
</CardGroup>
