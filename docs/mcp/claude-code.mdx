---
title: Deckflow MCP in Claude Code
sidebarTitle: Claude Code
description: "Configure Deckflow MCP in Claude Code with hosted HTTP OAuth or local stdio for presentation-processing workflows."
---

Use this page to connect Deckflow MCP in Claude Code. Hosted HTTP is the best default when you want browser-based OAuth; use local stdio only when the agent must pass local absolute file paths.

<Note>
  Prefer hosted HTTP for normal use. Switch to local stdio only when your workflow depends on local files that are not available as URLs or uploaded `fileId:<id>` references.
</Note>

## Hosted HTTP setup

<Steps>
  <Step title="Add the Deckflow MCP endpoint">
    ```bash
    claude mcp add --transport http deckflow https://mcp.deckflow.com/mcp/v1/
    ```
  </Step>
  <Step title="Authorize in Claude Code">
    Run `/mcp` in Claude Code and follow the browser authorization prompt. The server supports MCP OAuth discovery, dynamic client registration, and PKCE.
  </Step>
  <Step title="Start a new Claude Code session">
    Ask Claude Code to list Deckflow tools or run a small task, such as rendering Markdown or converting a hosted file URL.
  </Step>
</Steps>

## Local stdio setup

Build the MCP server locally, then register the stdio entry:

```bash
npm install
npm run build

claude mcp add deckflow \
  -e DECKFLOW_API_KEY=<key> \
  -- node /absolute/path/deckflow-mcp/dist/entry/stdio.js
```

Add `DECKFLOW_SPACE_ID=<spaceId>` when you want all calls to use a specific Deckflow space.

## File input behavior

| Transport | Accepted file inputs |
|---|---|
| Hosted HTTP | `http(s)` URLs and `fileId:<uploaded-file-id>` |
| Local stdio | Local absolute paths, `http(s)` URLs, and `fileId:<uploaded-file-id>` |

## What to try after setup

- Convert an HTML slide page into an editable PPTX deck.
- Convert a PPTX file into PDF or image outputs for sharing.
- Translate a PPTX, PDF, or DOCX file, including text inside images, while preserving layout where possible.
- Generate a first-draft presentation from a short brief plus reference files or URLs.

## Example prompts

```text
Use Deckflow MCP to convert this HTML page into an editable PPTX deck and return the download link.
```

```text
Convert this PPTX to PDF, then compress the result if it is still too large to share.
```

```text
Create a presentation for a live meeting from this brief and the attached PDF and Markdown references.
```

## Validation checklist

- Claude Code lists the `deckflow` server in `/mcp`.
- Tool discovery shows generation, translation, rendering, file utility, and task management tools.
- A small utility task returns either a completed result or a `taskId`.
- Long-running tasks can be checked with `get_task` and downloaded with `download_task_result`.
