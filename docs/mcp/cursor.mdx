---
title: Deckflow MCP in Cursor
sidebarTitle: Cursor
description: "Configure Deckflow MCP in Cursor for HTML-to-PPTX, format conversion, and presentation-processing workflows."
---

Use this page to connect Deckflow MCP in Cursor. Hosted HTTP is the best default when your Cursor build supports remote MCP servers; switch to local stdio only when the MCP server must read local absolute file paths.

<Note>
  Cursor MCP configuration may vary by version. Start with hosted HTTP unless you specifically need local file path access.
</Note>

## Hosted HTTP endpoint

```text
https://mcp.deckflow.com/mcp/v1/
```

Use one of these authentication paths:

| Method | When to use it |
|---|---|
| OAuth | Cursor supports remote MCP authorization in your environment. |
| `Authorization: Bearer <API key>` | Cursor or a proxy can send custom headers. |
| `?API_KEY=<key>` | Custom headers are not available. |

## Local stdio configuration

```json
{
  "mcpServers": {
    "deckflow": {
      "command": "node",
      "args": ["/absolute/path/deckflow-mcp/dist/entry/stdio.js"],
      "env": {
        "DECKFLOW_API_KEY": "<your-api-key>"
      }
    }
  }
}
```

Add `DECKFLOW_SPACE_ID` if you want Cursor to operate inside a fixed Deckflow space.

## What to try after setup

- Convert an HTML slide page into an editable PPTX deck.
- Convert a PPTX file into PDF or image outputs for sharing.
- Translate a PPTX, PDF, or DOCX file, including text inside images, while preserving layout where possible.
- Generate a first-draft presentation from a short brief plus reference files or URLs.

## Prompt examples

```text
Use Deckflow MCP to convert docs/examples/intro.html into an editable PPTX deck and return the download link.
```

```text
Convert this PPTX to PDF, then compress the result if it is still too large to share.
```

```text
Create a presentation for a live meeting from this brief and the attached PDF and Markdown references.
```

## Troubleshooting

| Symptom | Check |
|---|---|
| Cursor cannot read a local file | Use stdio mode, or upload the file and pass `fileId:<id>`. |
| Authorization fails | Prefer OAuth, or verify the API key header/query value. |
| Task does not finish before client timeout | Ask Cursor to call `get_task` later, or use a shorter `waitSeconds` and poll. |
